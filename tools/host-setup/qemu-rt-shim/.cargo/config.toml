# Build for the host, NOT the kernel.  Overrides the parent .cargo/config
# that targets aarch64-unknown-none + build-std.
[build]
target = "x86_64-unknown-linux-gnu"

[unstable]
build-std = []
build-std-features = []
