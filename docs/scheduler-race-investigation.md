# Scheduler race investigation (Track 3 analysis)

Under heavy boot load (large initramfs, many concurrent FS / Linux-personality
servers, Xwayland-class workloads), Telix's kernel scheduler reproducibly hits
an IPC stall watchdog with the following signature:

    WATCHDOG: IPC stall detected (sends=N recvs=N) double_enq: drain=0
              rescue=0 wake=0 other=0 total_enq=… sgi=(s=0 r=0)

repeated with the **same** N for 5 + seconds.  Per-thread state dumps show one
or more threads in:

    state=Ready  blocked_on=CallReply(slot)  on_cpu=ON_CPU_PENDING
    in_queue=true  wakeup=false  park_state=0

while the CPU's `current_thread` is the same stuck tid (`cur=tidN
idle=false rq_eevdf=K has_ready=true`).  Existing recovery (drain
deferred-requeue, reschedule IPIs, rescue orphans) doesn't unstick them — the
`double_enq` counters all remain zero.

## Hypotheses generated by static analysis

The Track 3 review of `kernel/src/sched/scheduler.rs` produced three
candidates, ordered by likelihood given current evidence:

1. **Lost run-queue insertion via stale `in_queue=true`.**  `percpu_enqueue` at
   line 852 swap-CASes `in_queue` to `true` and early-returns if it was already
   `true`.  If the heap insert at line 903 ever fails-with-fallback or is
   raced against `remove_tid`, `in_queue` can be left `true` while the thread
   is no longer in any actual queue.  The next wake-up calls `percpu_enqueue`,
   sees `in_queue=true`, returns without inserting → the thread is `state=
   Ready` but not on any queue → rescue at line 5356 requires `!inq` to act,
   so it walks past it.

   **Discriminator:** the early-return path increments `DOUBLE_ENQ_WAKE`.  If
   `wake>0` in the watchdog dump, this is the bug.

2. **`wake_parked_thread` CAS noop.** The CAS at line 5188 from
   `PARK_COMMITTED → PARK_NONE` may fail when an early-wake or abandon already
   cleared `park_state`.  The function logs `WAKE-PARK-NOOP` and returns
   without running `percpu_enqueue`.

   **Discriminator:** grep dmesg for `WAKE-PARK-NOOP`.

3. **Race in dispatch publish window.**  Lines 1568-1609 store
   `in_queue=false` *before* `dequeue_set_pending` publishes
   `on_cpu=ON_CPU_PENDING`.  In the gap, rescue could see `on_cpu=cpu_real`
   (left over from a prior run), `cur != tid`, `in_queue=false`, `state=
   Ready`, declare an orphan, and re-enqueue a thread that is *right now*
   being dispatched.

## Discriminating data from existing logs

Logs `/tmp/h13gg.log`, `/tmp/h13hh.log`, `/tmp/h13dd.log` all show:

    double_enq: drain=0 rescue=0 wake=0 other=0

i.e. `wake=0`.  That **rules out hypothesis 1** (lost insert) and is also
silent on hypothesis 2 — no `WAKE-PARK-NOOP` strings appear.  Hypothesis 3
remains plausible but the `rescue=0` counter rules out spurious orphan
re-enqueues having fired.

The actual stuck-thread observation — `on_cpu=ON_CPU_PENDING` while
`pcpu.current_thread == tid` — suggests **the CPU never actually returned to
its scheduler dispatch path** after some syscall / IRQ on tidN.  The `cur=
tidN` in the dump is stale because nothing has updated it.  That points
*away* from the ordering hypotheses and toward a missed reschedule on the
CPU itself: timer IRQ drop, masked interrupts in a syscall fast path, or a
spinwait holding the CPU.

## Suggested next investigation steps

- **Track 1**: confirm `wake=0` and absence of `WAKE-PARK-NOOP` in fresh
  reproductions (sched-stress runs).  Capture the per-CPU state at stall
  time — specifically whether `pcpu.current_thread` ever changes after the
  stall begins.
- Add a per-CPU "reschedules since last stall" counter to the watchdog dump.
  If the stuck CPU's counter is also frozen, the issue is "CPU is not
  reaching `try_switch`" (timer / IRQ-mask bug) rather than
  scheduler-state-machine bug.
- Static analysis re-pass on the **call/reply syscall return path** (look in
  `kernel/src/syscall/handlers.rs::sys_reply` / `sys_reply_to`) — does the
  reply syscall always re-enable preemption / yield before returning to the
  reply-receiver?

## Reproducer

`userlib/bin/sched_stress.rs` (Phase 5h in `init.rs`): 8 worker threads in
one task hammer a single echo server with `call`/`reply` round-trips.  Driver
heartbeat exits non-zero if no global progress for 20 s.  Designed to trip
the race in seconds rather than 25 minutes of boot.

`tools/scheduler-repro-loop.sh` runs H13 (with `STEP_H_FOCUS_H13=true`) in
a tight loop, archiving each log to `/tmp/sched-runs/run-NNN.log` and a
rolling summary to `/tmp/sched-runs/SUMMARY.txt`.

## Status

This is investigation-time analysis from one snapshot of the source.  The
bug is real, reproducible (eventually), and not yet diagnosed precisely.
The fixes proposed in Track 3's report (mostly "make `in_queue` strictly
self-consistent") are defensible regardless of whether the stall stems from
the queue-insertion path or the CPU-reschedule path; both are areas where
the current scheduler relies on more invariants than it actively enforces.
